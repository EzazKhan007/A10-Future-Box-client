# CleanCity — Client

## Setup
1. Copy `.env.example` to `.env` and set `VITE_API_BASE` to your backend base URL, e.g. `http://localhost:5000/api`.
2. Provide Firebase config in `src/firebase.js`.
3. Install & run:
   - `npm install`
   - `npm run start` (Vite)
